import { ReactNode } from "react";

export type SupportUsCardType = {
  icon: ReactNode;
  title: string;
  desc: string;
  content: ReactNode;
};
export type WhySupportUsItemType = {
  icon: ReactNode;
  title: string;
  bg: string;
  desc: string;
};